package com.lukariagroup.app.ui.screens.patient

/**
 * Android placeholder — wire CameraX + ML Kit / ZXing in a later milestone.
 * Returns null so the UI falls back to manual barcode entry.
 */
actual suspend fun scanBarcode(): String? = null
