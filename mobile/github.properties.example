# Copy to github.properties and fill in (github.properties is gitignored).
# Create a GitHub PAT with read:packages scope:
# https://github.com/settings/tokens
gpr.usr=YOUR_GITHUB_USERNAME
gpr.key=YOUR_GITHUB_PAT_WITH_READ_PACKAGES
